pre-commit:
  commands:
    format:
      glob: "*.{css,json,jsonc,md,mdx,yaml,yml}"
      run: bun run prettier --write {staged_files}
      stage_fixed: true

pre-push:
  commands:
    format:
      glob: "*.{css,json,jsonc,md,mdx,yaml,yml}"
      run: bun run prettier --check {push_files}
